<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pat.simswap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="MTN GH Sim Swap" />
        <activity
            android:name=".VerificationDetail"
            android:label="Customer Profile" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Sim_swap_request"
            android:label="SIM Swap Request" />
        <activity
            android:name=".Sim_swap_Image_Attachment"
            android:label="SIM Swap Request Attachment"></activity>
        <activity android:name=".Sim_request_summary"
            android:label="SIM Swap Summary">

        </activity>
    </application>

</manifest>